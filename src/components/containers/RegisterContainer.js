import React, { Component } from "react";
import { RegisterPage } from "../layout";

export class RegisterContainer extends Component {
  render() {
    return (
      <div>
        <RegisterPage />
      </div>
    );
  }
}

export default RegisterContainer;
