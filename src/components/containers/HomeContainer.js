import React, { Component } from "react";
import { Carousal } from "../layout";

export class HomeContainer extends Component {
  render() {
    return (
      <div>
        <Carousal />
      </div>
    );
  }
}

export default HomeContainer;
